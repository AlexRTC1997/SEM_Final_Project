import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'GES_Big_Times_Projects',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
